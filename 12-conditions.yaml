- name: demo on conditions  # Ansible choose keyword-> "when" for conditions
  hosts: localhost
  connection: local
  vars:
    IS_LIVE: false
  tasks:
  - name: check Live or not
    ansible.builtin.debug:
     msg: "Yes, it is Live"
    when: IS_LIVE