- name: demo on loop
  hosts: localhost
  connection: local
  tasks: 
  - name: print names
    ansible.builtin.debug:
     msg: "Hello {{ item }}"  # item is reserved keyword 
    loop:
    - Akvik
    - AK
    - sathosie